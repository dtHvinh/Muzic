meta {
  name: Delete Song
  type: http
  seq: 24
}

delete {
  url: {{baseUrl}}/songs?id={{songId}}
}

headers {
  Accept: application/json
}

tests {
  expect([200, 400, 404]).to.include(res.status);
}
