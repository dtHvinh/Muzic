meta {
  name: Remove Song From Playlist
  type: http
  seq: 37
}

post {
  url: {{baseUrl}}/playlists?mode=remove-song&playlistId={{playlistId}}&songId={{songId}}
}

headers {
  Accept: application/json
}

tests {
  expect([200, 400, 404]).to.include(res.status);
}
