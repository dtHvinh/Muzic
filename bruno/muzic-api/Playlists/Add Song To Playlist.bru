meta {
  name: Add Song To Playlist
  type: http
  seq: 36
}

post {
  url: {{baseUrl}}/playlists?mode=add-song&playlistId={{playlistId}}&songId={{songId}}
}

headers {
  Accept: application/json
}

tests {
  expect([200, 400, 404]).to.include(res.status);
}
