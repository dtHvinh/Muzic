apiVersion: v1
kind: Service
metadata:
  name: {{ include "muzic-server-charts.fullname" . }}
  labels:
    {{- include "muzic-server-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "muzic-server-charts.selectorLabels" . | nindent 4 }}
