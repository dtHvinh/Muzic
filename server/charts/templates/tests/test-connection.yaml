apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "muzic-server-charts.fullname" . }}-test-connection"
  labels:
    {{- include "muzic-server-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "muzic-server-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
